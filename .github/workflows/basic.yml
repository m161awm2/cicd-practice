name: cicd-test01

on:
  push:
    branches:
      - main

jobs:
  echo-test:
    runs-on: ubuntu-latest
    steps:
      - name: download repo into runner
        uses: actions/checkout@v4
      - name: echo-test
        run: echo "안녕하시긔"
      
